The previous report was a browser fallback result. Seven routes, two vehicles,
two drivers and one report proved that the operational APIs and database data
were still present. Stage 1.2.1 inspects the live schema before querying newer
Company columns or optional tables and exposes exact errors.
